import React, { useMemo } from 'react';
import { createTheme, CssBaseline, GlobalStyles, ThemeProvider } from '@mui/material';
import { UserContextProvider } from './contexts/UserContext';
import { EventContextProvider } from './contexts/EventContext';
<<<<<<< HEAD
import { AppRouter } from '../routes/AppRouter';
import RegisterToEventForm from './pages/common/DialogRegisterEvent';
import { BrowserRouter } from 'react-router-dom';
=======
import { EventRouter } from '../routes/EventRouter';
>>>>>>> 5c99a50 (rebase?)

export const App = () => {
  const theme = useMemo(
    () =>
      createTheme({
        palette: {
          primary: {
            light: 'white',
            main: '#42a5f5',
            dark: '#0d47a1',
            contrastText: '#e3f2fd'
          },
          secondary: {
            light: '#ff7961',
            main: '#42a5f5',
            dark: '#000000',
            contrastText: '#000'
          }
        }
      }),
    []
  );

  const globalStyles = useMemo(
    () => (
      <GlobalStyles
        styles={{
          html: { height: '100%' },
          body: { height: '100%' },
          '#root': { height: '100%' }
        }}
      />
    ),
    []
  );
  return (
    <ThemeProvider theme={theme}>
      <CssBaseline />
      {globalStyles}
<<<<<<< HEAD
      <BrowserRouter>
        <UserContextProvider>
          <EventContextProvider>
            <AppRouter />
          </EventContextProvider>
            <Route path='registerToEvent' element={<RegisterToEventForm />} />
        </UserContextProvider>
      </BrowserRouter>
=======
      <UserContextProvider>
        <EventContextProvider>
          <EventRouter />
        </EventContextProvider>
      </UserContextProvider>
>>>>>>> 5c99a50 (rebase?)
    </ThemeProvider>
  );
};
